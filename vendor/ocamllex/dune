(library
 (name ocamllexSyntax)
 (libraries menhirLib menhirCST pprint menhir_lsp_lib)
 (preprocess
  (pps visitors.ppx)))

(copy_files ../common/*.ml)

(env
 (dev
  (flags :standard -warn-error -7-17 -w -27)))

(ocamllex lexer)

(menhir
 (modules parser)
 (flags --table --unparsing))
