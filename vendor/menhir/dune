(library
 (name menhirSyntax)
 (libraries menhirSdk menhirLib unix)
 (preprocess
  (pps visitors.ppx)))

(env
 (dev
  (flags :standard -warn-error -7-17)))

(copy_files ../common/*.ml)

(ocamllex Lexer Lex Lexpointfree LineCount DecodeString)

(menhir
 (modules Parser))
