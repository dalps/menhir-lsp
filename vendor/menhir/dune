(library
 (name menhirSyntax)
 (libraries menhirSdk menhirLib unix menhir_lsp_lib)
 (preprocess
  (pps visitors.ppx)))

(env
 (dev
  (flags :standard -warn-error -7-17)))

(copy_files ../common/*.ml)

(ocamllex Lexer Lex Lexpointfree LineCount DecodeString)

(menhir
 (modules Parser))
